<core:FragmentDefinition xmlns="sap.m"
    xmlns:core="sap.ui.core"
    xmlns:f="sap.ui.layout.form">

    <Dialog id="CashFlowDialog" core:require="{handler:'cashflowcontractinitial/ext/controller/CalCashFLow'}" title="Cash Flow Details" afterOpen="handler.onDialogAfterOpen" contentWidth="80%" contentHeight="50%" resizable="true">

        <content>
            <VBox renderType="Bare" class="dialogVBox">

                <!-- Form Section -->
                <f:SimpleForm editable="false" layout="ResponsiveGridLayout" class="fixedForm">
                    <f:content>
                        <Label text="Loan Number" design="Bold" />
                        <Text text="{loanNumber}" />

                        <Label text="Product Type" design="Bold" />
                        <Text text="{productType}" />

                        <Label text="Status" design="Bold" />
                        <Text text="{status}" />

                        <Label text="Disb.Status" design="Bold" />
                        <Text text="{disbursementStatus}" />

                        <Label text="Loan Partner" design="Bold" />
                        <Text text="{loanPartner}" />
                    </f:content>
                </f:SimpleForm>

                <!-- Table Section -->
                <ScrollContainer horizontal="true" vertical="true" height="100%" width="100%" class="tableScroll">
                    <Table headerText="Loan Amortization" items="{/AmortizationSchedule}" inset="false" growing="true" growingScrollToLoad="false" class="conditionTableCls">

                        <columns>
                            <Column>
                                <header>
                                    <Label text="Payment Date" />
                                </header>
                            </Column>
                            <Column>
                                <header>
                                    <Label text="Name" />
                                </header>
                            </Column>
                            <Column>
                                <header>
                                    <Label text="Settlement Amount" />
                                </header>
                            </Column>
                            <Column>
                                <header>
                                    <Label text="Currency" />
                                </header>
                            </Column>
                        </columns>


                        <items>
                            <ColumnListItem>
                                <cells>
                                    <Text text="{paymentDate}" />
                                    <Text text="{name}" />
                                    <Text text="{settleAmount}" />
                                    <Text text="{currency}" />
                                </cells>
                            </ColumnListItem>
                        </items>
                    </Table>


                </ScrollContainer>
            </VBox>
        </content>

        <beginButton>
            <Button text="Close" press="handler.onClosePress" />
        </beginButton>
    </Dialog>
</core:FragmentDefinition>
